📌 Project Overview

This is a simple Random Number Generator built using React. It generates a random number between 1 and 100 when the user clicks a button.


🛠️ Technologies Used

⚛️ React

🎨 Tailwind CSS

🧠 JavaScript (Math.random & Math.floor)


⚙️ How It Works

Uses JavaScript’s Math.random() to generate a random decimal number between 0 and 1.

Uses Math.floor() to convert the decimal into a whole number.

The final logic ensures the number is between 1 and 100.

📂 Learning Outcome

Practiced React state management

Understood random number generation in JavaScript

Improved Tailwind CSS layout and centering techniques
